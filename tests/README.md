# `tests/`

This directory contains all tests for **SDE-Lab-packages**.

## Contents

| File / Folder | Description |
|---|---|
| *(to be added)* | Unit tests, integration tests, and test fixtures |

## Running Tests

```bash
# Example (update this once a test runner is configured)
bash tests/run_tests.sh
```

## Guidelines

- Every new feature or script should have corresponding tests.
- Tests must be executable in a clean Debian 13 (Trixie) environment.
- Name test files with a `test_` prefix (e.g., `test_install.sh`).
