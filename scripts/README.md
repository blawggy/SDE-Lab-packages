# `scripts/`

This directory contains shell scripts used for bootstrapping, installing, and managing lab environments on **Debian 13 (Trixie)**.

## Contents

| Script | Description |
|---|---|
| *(to be added)* | Installation and setup automation scripts |

## Usage

All scripts should be executed with appropriate privileges. For example:

```bash
sudo bash scripts/install.sh
```

## Guidelines

- Scripts must be POSIX-compatible where possible.
- Include a brief header comment in each script describing its purpose, usage, and any required privileges.
- Test scripts in a clean Debian 13 environment before committing.
